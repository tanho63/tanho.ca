# section information
section:
  name: Recent Posts
  id: recent-posts-rowing
  enable: true
  weight: 6
  showOnNavbar: false
  # Can optionally hide the title in sections
  # hideTitle: true

# no additional configuration is required
